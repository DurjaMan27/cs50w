name = "Harry"
print(name[0]) # prints the first character of the variable name

names = ["Harry", "Ron", "Hermione"] # list
print(names[2])

coordinate = (10.0, 20.0) # tuple - used when you have a pair of values that aren't going to change
